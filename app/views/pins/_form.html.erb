<%= form_for(@pin) do |f| %>
  <% if @pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% @pin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :board_id %><br>
    <%= f.text_field :board_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>

  <!-- <%= image_tag(@pin.image.thumb.url) if @pin.image? %> -->
  <div class="field">
    <%= f.label :movie_poster %>
    <%= f.file_field :movie_poster %>
    <%= f.hidden_field :movie_poster_cache %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
